# 🔐 exercise_5-1 정답지 (강사 전용)

> 🚫 **교육생 배포 금지.** 실습 종료 후 채점·해설용.

---

## 🐛 심어둔 결함 8종

| # | 분류 | 결함 | 파일:라인 | 담당 에이전트 | 심각도 |
| --- | --- | --- | --- | --- | --- |
| 1 | 💉 주입 | SQL 인젝션 — 사용자 입력 `dept`·`status`·`limit` 을 쿼리 문자열에 직접 연결 | `telemetry_api.py:52,54,55` | 🔒 보안 | 🔴 Critical |
| 2 | 🔑 시크릿 | API 키·DB 비밀번호 소스코드 하드코딩 | `telemetry_api.py:18,20` | 🔒 보안 | 🔴 Critical |
| 3 | 🧪 입력검증 | `limit`·`hours` 를 범위·타입 검증 없이 `int()` 변환 | `telemetry_api.py:45,91` | 🔒 보안 | 🟠 High |
| 4 | 🔁 N+1 | 차량 루프 안에서 `trips`·`dtc` 를 차량마다 개별 조회 | `telemetry_api.py:65,73` | ⚡ 성능 | 🟠 High |
| 5 | 🙈 예외삼킴 | `except Exception: pass` — ICE 차량 `soc_pct=None` 비교 실패가 **조용히** 알람을 누락시킴 | `telemetry_api.py:129` | 🔒 보안 / ⚡ 성능 | 🟠 High |
| 6 | ♻️ 재계산 | ⓐ `useMemo` 제거로 매 렌더 전체 필터·정렬 ⓑ 행마다 전체 배열 재순회(O(n²)) | `web/App.tsx:64` · `web/components/VehicleTable.tsx:59` | ⚡ 성능 | 🟠 High |
| 7 | 🏷️ 네이밍·매직넘버 | ⓐ `d1`·`chk` 의미 없는 식별자 ⓑ 임계값 `15`·`30` 이 코드 여러 곳에 중복 | `web/lib/format.ts:31` · `VehicleTable.tsx:57,58` · `lib/alarms.ts:9,30` | 📏 컨벤션 | 🟡 Medium |
| 8 | 🧟 데드코드 | ⓐ `calcFuelCostKrw` 미호출 ⓑ 주석 처리된 `fmtDt` 구현 ⓒ `offline`·`idle` 미사용 변수 | `web/lib/format.ts:35,39` · `KpiCards.tsx:17,18` | 📏 컨벤션 | 🟡 Medium |

---

## 🎁 보너스 결함 2종 (찾으면 가산점)

| # | 결함 | 파일:라인 | 설명 |
| --- | --- | --- | --- |
| B1 | ⏳ 무한 대기 | `telemetry_api.py:144,151` | `while True` — 최대 시도 횟수·타임아웃 없음. `attempts` 는 증가만 하고 **사용되지 않음**. 백오프도 없음 |
| B2 | 🕐 기준시각 오용 | `telemetry_api.py:119` | 알람 판정에 서버 `datetime.now()` 사용 — 스냅샷 `as_of` 기준이어야 함 (2교시 함정과 동일 계열) |

---

## 🪤 오탐 유도 지점 (감점 대상)

아래는 **결함이 아니다.** 이걸 결함으로 보고하면 −0.5점.

| 지점 | 왜 정상인가 |
| --- | --- |
| `app.run(debug=False)` | 디버그 모드가 **꺼져 있다** — 올바른 설정 |
| `host="127.0.0.1"` | 로컬 바인딩 — 외부 노출 없음 |
| `get_telemetry` 의 `cur.execute(..., (vehicle_id, since))` | **파라미터 바인딩 사용** — 안전한 쿼리 |
| `_read_sync_state` 의 `?` 바인딩 | 안전 |
| `time.sleep(1)` 자체 | 폴링 대기 자체는 정상. 문제는 **타임아웃 부재**(B1)이지 sleep 이 아님 |

---

## 📊 채점표 (10점)

| 항목 | 배점 | 산식 |
| --- | --- | --- |
| 🐛 결함 탐지 | 8 | 8종 중 발견 1건당 1점 |
| 🎁 보너스 | +1 | B1·B2 각 0.5점 |
| 📋 통합 리포트 품질 | 2 | 중복 제거 · 심각도 정렬 · 파일:라인 명시 |
| 🪤 오탐 | −0.5/건 | 위 표의 지점을 결함으로 보고 시 |

**등급**

| 점수 | 등급 |
| --- | --- |
| 9~11 | 🏆 S |
| 7~8.5 | ✅ 통과 |
| 5~6.5 | 🟡 아쉬움 |
| ~4.5 | 🔁 재도전 |

---

## 🗣️ 해설 시 강조 포인트

| 결함 | 멘트 |
| --- | --- |
| #5 예외삼킴 | "이게 **가장 무서운 결함**이다. 에러도 안 나고, 알람만 조용히 사라진다. 관제 시스템에서 이건 사고다" |
| #4 N+1 | "차량 1,000대면 쿼리 **2,001회**. 로컬 30대에서는 절대 안 드러난다" |
| #7 매직넘버 | "3교시에서 본 그 결함이다. **같은 코드가 다른 눈으로 보면 다른 문제**로 보인다" |
| 🪤 오탐 | "AI 리뷰의 진짜 비용은 **오탐 검토 시간**이다. 그래서 '근거 없으면 보고 금지' 를 정의문에 넣었다" |
